- name: variable playbook example
  hosts: all
  vars:
    URL: "global.google.com"
    var_files: "HITESH MANKAR"
  tasks: 
     - name: print message
       debug:
          msg: "Welcome TO {{ URL }}, my name is {{ VAR1 }}"
          