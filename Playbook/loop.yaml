- name: Loop in ansible
  hosts: all
  tasks:
     - name: loop
       debug:
         msg: 'my fav fruit {{ items }}'
         loop:
            - mango:
            - banana:
            - apple:
            - orange:  